/**
 * NOTA DE SEGURANÇA:
 * Este arquivo foi esvaziado intencionalmente.
 * 
 * NUNCA coloque chaves de API (WhatsApp/Email) no código JavaScript 
 * do navegador (Frontend), pois qualquer usuário pode roubá-las.
 * 
 * As notificações devem ser disparadas pelo Google Apps Script (Backend)
 * após receber os dados do formulário.
 */

async function sendAllNotifications(leadData) {
    // O Backend (Google Script) cuidará disso.
    return true; 
}